{
  "name": "codex-chrome-mcp",
  "version": "1.0.0",
  "description": "MCP server exposing Codex's Chrome (browser use) plugin as tools, by bridging the bundled browser-client over Codex's native pipe",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "codex-chrome-mcp": "./bin/codex-chrome-mcp.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "keywords": ["mcp", "browser-use", "chrome", "codex", "cursor"],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
